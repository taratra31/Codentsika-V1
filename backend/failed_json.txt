=== RAW AI RESPONSE (first 10000 chars) ===
{
  "project_name": "flight_reservation_system",
  "description": "Système de réservation de vols avec passagers, escales et tickets",
  "files": [
    {
      "path": "app/main.py",
      "content": "from fastapi import FastAPI, Depends, HTTPException\nfrom fastapi.middleware.cors import CORSMiddleware\nfrom fastapi.responses import JSONResponse\nfrom fastapi.security import OAuth2PasswordBearer, OAuth2PasswordRequestForm\nfrom fastapi_lifespan import LifespanManager\nfrom sqlalchemy.orm import declarative_base, scoped_session\nfrom sqlalchemy.orm.session import sessionmaker\nfrom app.database import engine, SessionLocal\nfrom app.dependencies import get_db, get_current_user\nfrom app.routers import flights, passengers, stops, tickets\n\napp = FastAPI(\n    title=\"Flight Reservation System\",\n    description=\"Système de réservation de vols avec passagers, escales et tickets\",\n    version=\"1.0.0\",\n    contact={\n        \"name\": \"Your Name\",\n        \"email\": \"your@email.com\"\n    }\n)\n\norigins = [\n    \"http://localhost:8000\",\n    \"http://localhost:3000\"\n]\n\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=origins,\n    allow_credentials=True,\n    allow_methods=[\"*\"],\n    allow_headers=[\"*\"],\n)\n\napp.include_router(flights.router)\napp.include_router(passengers.router)\napp.include_router(stops.router)\napp.include_router(tickets.router)\n\n@app.on_event(\"startup\")\nasync def startup_event():\n    await engine.dispose()\n\n@app.on_event(\"shutdown\")\ndef shutdown_event():\n    pass\n\n@app.exception_handler(404)\ndef not_found_exception_handler(request, exc):\n    return JSONResponse(status_code=404, content={\"message\": \"Not Found\"})\n\n@app.exception_handler(422)\ndef unprocessable_entity_exception_handler(request, exc):\n    return JSONResponse(status_code=422, content={\"message\": \"Unprocessable Entity\"})\n"
    },
    {
      "path": "app/database.py",
      "content": "from sqlalchemy import create_engine\nfrom sqlalchemy.orm import declarative_base, scoped_session\nfrom sqlalchemy.orm.session import sessionmaker\nfrom app.config import DATABASE_URL\n\nengine = create_engine(DATABASE_URL)\n\nBase = declarative_base()\n\nSessionLocal = scoped_session(sessionmaker(bind=engine, autocommit=False, autoflush=False))\n\ndef get_db():\n    db = SessionLocal()\n    try:\n        yield db\n    finally:\n        db.close()\n"
    },
    {
      "path": "app/models.py",
      "content": "from sqlalchemy import Column, Integer, String, DateTime, ForeignKey\nfrom sqlalchemy.orm import relationship\nfrom app.database import Base\n\nclass Flight(Base):\n    __tablename__ = \"flights\"\n\n    id = Column(Integer, primary_key=True)\n    departure = Column(String)\n    arrival = Column(String)\n    departure_time = Column(DateTime)\n    arrival_time = Column(DateTime)\n\n    stops = relationship(\"Stop\", back_populates=\"flight\")\n    tickets = relationship(\"Ticket\", back_populates=\"flight\")\n\n    def __repr__(self):\n        return f\"Flight(id={self.id}, departure={self.departure}, arrival={self.arrival})\"\n\nclass Passenger(Base):\n    __tablename__ = \"passengers\"\n\n    id = Column(Integer, primary_key=True)\n    name = Column(String)\n    email = Column(String)\n\n    tickets = relationship(\"Ticket\", back_populates=\"passenger\")\n\n    def __repr__(self):\n        return f\"Passenger(id={self.id}, name={self.name}, email={self.email})\"\n\nclass Stop(Base):\n    __tablename__ = \"stops\"\n\n    id = Column(Integer, primary_key=True)\n    flight_id = Column(Integer, ForeignKey(\"flights.id\"))\n    stop_time = Column(DateTime)\n\n    flight = relationship(\"Flight\", back_populates=\"stops\")\n\n    def __repr__(self):\n        return f\"Stop(id={self.id}, flight_id={self.flight_id}, stop_time={self.stop_time})\"\n\nclass Ticket(Base):\n    __tablename__ = \"tickets\"\n\n    id = Column(Integer, primary_key=True)\n    flight_id = Column(Integer, ForeignKey(\"flights.id\"))\n    passenger_id = Column(Integer, ForeignKey(\"passengers.id\"))\n\n    flight = relationship(\"Flight\", back_populates=\"tickets\")\n    passenger = relationship(\"Passenger\", back_populates=\"tickets\")\n\n    def __repr__(self):\n        return f\"Ticket(id={self.id}, flight_id={self.flight_id}, passenger_id={self.passenger_id})\"\n"
    },
    {
      "path": "app/schemas.py",
      "content": "from pydantic import BaseModel, EmailStr\nfrom datetime import datetime\n\nclass FlightBase(BaseModel):\n    departure: str\n    arrival: str\n    departure_time: datetime\n    arrival_time: datetime\n\nclass FlightCreate(FlightBase):\n    pass\n\nclass FlightResponse(FlightBase):\n    id: int\n\n    class Config:\n        orm_mode = True\n\nclass PassengerBase(BaseModel):\n    name: str\n    email: EmailStr\n\nclass PassengerCreate(PassengerBase):\n    pass\n\nclass PassengerResponse(PassengerBase):\n    id: int\n\n    class Config:\n        orm_mode = True\n\nclass StopBase(BaseModel):\n    flight_id: int\n    stop_time: datetime\n\nclass StopCreate(StopBase):\n    pass\n\nclass StopResponse(StopBase):\n    id: int\n\n    class Config:\n        orm_mode = True\n\nclass TicketBase(BaseModel):\n    flight_id: int\n    passenger_id: int\n\nclass TicketCreate(TicketBase):\n    pass\n\nclass TicketResponse(TicketBase):\n    id: int\n\n    class Config:\n        orm_mode = True\n"
    },
    {
      "path": "app/dependencies.py",
      "content": "from fastapi import Depends, HTTPException\nfrom fastapi.security import OAuth2PasswordBearer, OAuth2PasswordRequestForm\nfrom app.database import SessionLocal\nfrom app.models import Passenger\n\noauth2_scheme = OAuth2PasswordBearer(tokenUrl=\"token\")\n\ndef get_db():\n    db = SessionLocal()\n    try:\n        yield db\n    finally:\n        db.close()\n\ndef get_current_user(token: str = Depends(oauth2_scheme)):\n    user = authenticate_user(token)\n    if not user:\n        raise HTTPException(status_code=401, detail=\"Invalid authentication credentials\")\n    return user\n\ndef authenticate_user(token: str):\n    # TO DO: implement authentication logic\n    pass\n"
    },
    {
      "path": "app/crud.py",
      "content": "from app.database import SessionLocal\nfrom app.models import Flight, Passenger, Stop, Ticket\n\ndef get_flights(db: SessionLocal):\n    return db.query(Flight).all()\n\ndef get_flight(db: SessionLocal, flight_id: int):\n    return db.query(Flight).filter(Flight.id == flight_id).first()\n\ndef create_flight(db: SessionLocal, flight: Flight):\n    db.add(flight)\n    db.commit()\n    db.refresh(flight)\n    return flight\n\ndef update_flight(db: SessionLocal, flight_id: int, flight: Flight):\n    db_flight = get_flight(db, flight_id)\n    if db_flight:\n        db_flight.departure = flight.departure\n        db_flight.arrival = flight.arrival\n        db_flight.departure_time = flight.departure_time\n        db_flight.arrival_time = flight.arrival_time\n        db.commit()\n        db.refresh(db_flight)\n        return db_flight\n    return None\n\ndef delete_flight(db: SessionLocal, flight_id: int):\n    db_flight = get_flight(db, flight_id)\n    if db_flight:\n        db.delete(db_flight)\n        db.commit()\n        return True\n    return False\n\n# TO DO: implement CRUD operations for Passenger, Stop, and Ticket\n"
    },
    {
      "path": "app/routers/flights.py",
      "content": "from fastapi import APIRouter, Depends, HTTPException\nfrom app.crud import get_flights, get_flight, create_flight, update_flight, delete_flight\nfrom app.dependencies import get_db\nfrom app.schemas import FlightCreate, FlightResponse\n\nrouter = APIRouter(prefix=\"/flights\", tags=[\"Flights\"])\n\n@router.get(\"/\")\ndef read_flights(db: SessionLocal = Depends(get_db)):\n    return get_flights(db)\n\n@router.post(\"/\", response_model=FlightResponse)\ndef create_flight(flight: FlightCreate, db: SessionLocal = Depends(get_db)):\n    return create_flight(db, flight)\n\n@router.get(\"/{flight_id}\")\ndef read_flight(flight_id: int, db: SessionLocal = Depends(get_db)):\n    db_flight = get_flight(db, flight_id)\n    if db_flight is None:\n        raise HTTPException(status_code=404, detail=\"Flight not found\")\n    return db_flight\n\n@router.put(\"/{flight_id}\", response_model=FlightResponse)\ndef update_flight(flight_id: int, flight: FlightCreate, db: SessionLocal = Depends(get_db)):\n    return update_flight(db, flight_id, flight)\n\n@router.delete(\"/{flight_id}\")\ndef delete_flight(flight_id: int, db: SessionLocal = Depends(get_db)):\n    if delete_flight(db, flight_id):\n        return {\"message\": \"Flight deleted successfully\"}\n    raise HTTPException(status_code=404, detail=\"Flight not found\")\n"
    },
    {
      "path": "app/routers/passengers.py",
      "content": "from fastapi import APIRouter, Depends, HTTPException\nfrom app.crud import get_passengers, get_passenger, create_passenger, update_passenger, delete_passenger\nfrom app.dependencies import get_db\nfrom app.schemas import PassengerCreate, PassengerResponse\n\nrouter = APIRouter(prefix=\"/passengers\", tags=[\"Passengers\"])\n\n@router.get(\"/\")\ndef read_passengers(db: SessionLocal = Depends(get_db)):\n    return get_passengers(db)\n\n@router.post(\"/\", response_model=PassengerResponse)\ndef create_passenger(passenger: PassengerCreate, db: SessionLocal = Depends(get_db)):\n    return create_passenger(db, passenger)\n\n@router.get(\"/{passenger_id}\")\ndef read_passenger(passenger_id: int, db: SessionLocal = Depends(get_db)):\n    db_passenger = get_passenger(db, passenger_id)\n    if db_passenger is None:\n        raise HTTPException(status_code=404, detail=\"Passenger not found\")\n    return db_passenger\n\n@router.put(\"/{passenger_id}\", response_model=PassengerResponse)\ndef update_passenger(passenger_id: int, passenger: PassengerCreate, db: SessionLocal = Depends(get_db)):\n    return update_passenger(db, passenger_id, passenger)\n\n@router.delete